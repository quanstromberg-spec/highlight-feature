import { default as ResponsiveValue } from '../../../base/responsive-value';
export declare const useCurrentResponsiveValue: <T>(value?: ResponsiveValue<T>) => (T & ({} | null)) | undefined;
export default useCurrentResponsiveValue;
